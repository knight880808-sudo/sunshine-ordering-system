# 阳光集团订货系统（新手操作指南）

这份文档给第一次使用系统的同事看。  
目标：不需要懂技术，照着步骤点就能完成工作。

---

## 1. 先了解三个角色

- **分店员工**：下单、看我的订单、确认收货、查看缺货处理结果
- **仓库员工**：看待发货订单、填写发货数量、处理缺货通知
- **管理员**：看全局数据、导出报表、发进货通知、管理邮件与图片、消息中心

---

## 2. 登录与基础操作

1. 打开系统后，先选择语言（EN/中文）。
2. 选择你的角色：
   - 分店员工：继续选分店后进入系统
   - 仓库/管理员：输入密码登录
3. 左侧是功能导航，点击任意菜单即可切换页面。
4. 右上角可切换语言。

---

## 3. 分店员工怎么用（最常用）

## 3.1 下单

进入左侧 `🛒 下单`：

1. 在搜索框输入**商品名/条码/编号**（支持扫码枪）。
2. 在商品卡右侧填写数量（箱数、个数）。
3. 点 `📥 追加购物车（已选商品）`。
4. 点 `📝 确认并发送订单`，核对后点 `📤 发送订单`。
5. 出现“订单已发送完毕”即成功。

提示：
- 下单页每页显示 5 个商品，可翻页继续选。
- 若有“新货已到货”提示，请优先下单该清单商品。

## 3.2 查看订单与确认收货

进入左侧 `📋 我的订单`：

1. 找到状态为 `已发货` 的订单。
2. 点击展开，核对仓库发货数量。
3. 填写实收数量后点 `✅ 确认收货`。

结果：
- 正常收货后订单变 `已收货`。
- 若实收少于发货，会自动形成缺货记录并通知仓库处理。

## 3.3 查看缺货处理进度

进入左侧 `🔔 我的缺货`：

- 可看到仓库处理状态（补发中/缺货/已解决）及回复信息。

## 3.4 看系统消息

进入左侧 `🔔 消息中心`：

- 查看新订单、发货、收货、缺货、到货通知等消息
- 可单条 `标记已读` 或 `全部标记已读`

---

## 4. 仓库员工怎么用

## 4.1 待发货订单

进入 `📦 待发货订单`：

1. 可按分店、日期、关键词筛选。
2. 展开某个订单，系统默认发货数=下单数。
3. 按实际库存调整发货数。
4. 点 `🚚 标记已发货`。

结果：
- 分店会在“我的订单”看到已发货并可确认收货
- 系统会发送通知（邮件 + 站内消息）

## 4.2 处理缺货通知

进入 `🔔 缺货通知`：

- 对每条缺货可选：
  - `♻️ 补发`（标记补发中）
  - `❌ 标记缺货`
- 可填写仓库回复，分店会在“我的缺货/消息中心”看到。

## 4.3 查看出库历史

进入 `📜 出库历史`：

- 按日期查看已发货订单、收货状态（待收/部分收货/全部收货）

---

## 5. 管理员怎么用

## 5.1 管理概览

进入 `📊 管理概览` 可查看：

- 今日订单
- 待发货数量
- 已发货数量
- 待处理缺货
- 分店状态与最近订单

## 5.2 进货通知（重点）

进入 `📦 进货通知`：

1. 填写到货标题、通知说明。
2. 在“到货商品清单”录入商品（每行一个）。
3. 可通过搜索和多选把商品**追加到清单**，减少手输。
4. 点击 `发布到货通知`。

效果：
- 分店下单页会显示“新货到货”提示与清单
- 会同步发到分店消息中心
- 新通知发布后会覆盖旧通知

## 5.3 导出报表

进入 `📥 导出报表`：

- 可导出捡货单、对账单、缺货报告（Excel）

## 5.4 邮件通知配置

进入 `📧 邮件通知`：

- 配置 SMTP、事件收件人、分店邮箱
- 支持发送测试邮件
- 可查看邮件发送日志

---

## 6. 消息与提醒规则（你需要知道）

- 系统支持站内消息：新订单、已发货、已收货、缺货、到货通知等
- 左侧 `消息中心` 会显示未读数量
- 处理业务后记得到消息中心确认，避免漏单

---

## 7. 常见问题（新手版）

## Q1：搜索不到商品？

- 先确认输入关键词是否正确（名称/条码/编号）
- 确认你在 `🛒 下单` 页面
- 尝试清空关键词重新搜

## Q2：订单已发货但分店没看到？

- 让分店到 `📋 我的订单` 刷新查看
- 检查是否真的点了 `🚚 标记已发货`
- 管理员可在邮件日志和消息中心核对

## Q3：为什么有缺货？

- 分店确认收货时，实收数量少于仓库发货数量
- 系统会自动生成缺货通知给仓库处理

---

## 8. 推荐操作习惯

- 分店：每天先看 `消息中心` 再下单
- 仓库：先处理 `待发货订单`，再处理 `缺货通知`
- 管理员：每次到货后第一时间发布 `进货通知`

---

如需给员工培训，建议把本文打印或发群，按“分店/仓库/管理员”三段讲解即可。

